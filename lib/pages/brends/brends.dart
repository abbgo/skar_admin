import 'package:flutter/material.dart';

class BrendsPage extends StatelessWidget {
  const BrendsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Brends')));
  }
}
