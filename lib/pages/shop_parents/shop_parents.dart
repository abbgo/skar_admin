import 'package:flutter/material.dart';

class ShopParents extends StatelessWidget {
  const ShopParents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Shop Parents'),
      ),
    );
  }
}
