import 'package:flutter/material.dart';

class TrashedShops extends StatelessWidget {
  const TrashedShops({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Dukanlar');
  }
}
