import 'package:flutter/material.dart';

class BottomNavigationPart extends StatelessWidget {
  const BottomNavigationPart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Bottom Navigation')));
  }
}
